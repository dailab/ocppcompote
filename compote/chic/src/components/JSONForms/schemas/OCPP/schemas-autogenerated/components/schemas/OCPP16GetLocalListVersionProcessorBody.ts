const schema = {
  type: "object",
  title: "OCPP16GetLocalListVersionProcessorBody",
} as const;
export default schema;

const with$id = {
  $id: "/components/schemas/OCPP16GetLocalListVersionProcessorBody",
  ...schema,
} as const;
export { with$id };
